# Template only. Do not commit real values: generate this from your secrets manager
# (External Secrets Operator, Vault Agent, or `kubectl create secret`) at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: sentinel-aegis-secrets
  namespace: sentinel-aegis
type: Opaque
stringData:
  # Mounted at /run/secrets and read through `secret://file/<name>` references.
  database_url: "postgresql+asyncpg://aegis:CHANGE_ME@postgres:5432/aegisai"
  redis_url: "redis://redis:6379/0"
  anthropic_api_key: "CHANGE_ME"
